#include "globalvars.hpp"
#include "spritesheet.hpp"
#include <raylib.h>

namespace spritesheet
{
    // to do implement
    Vector4 GetSpritePosFromSpritesheet(Sprites sprite)
    {
        switch (sprite) //todo
        {
            default:
                 break;
        }
        return Vector4{0, 0, 0, 0};
    }

    bool DrawSpriteFromPos(Vector4 xywh,Vector2 dest,Color color)
    {
        return false;
    }
}